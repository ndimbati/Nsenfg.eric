# System Architecture

## 📐 Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Login.jsx  │  │ Register.jsx │  │  Home.jsx    │         │
│  │              │  │              │  │              │         │
│  │ POST /login  │  │ POST /register│ │ GET /content │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                 │                 │                  │
│  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴───────┐         │
│  │   Team.jsx   │  │  About.jsx   │  │ Contact.jsx  │         │
│  │              │  │              │  │              │         │
│  │ GET /content │  │ GET /content │  │ GET /content │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                 │                 │                  │
│         │          ┌──────┴───────┐         │                  │
│         │          │  Footer.jsx  │         │                  │
│         │          │              │         │                  │
│         │          │ GET /content │         │                  │
│         │          └──────┬───────┘         │                  │
│         │                 │                 │                  │
└─────────┼─────────────────┼─────────────────┼──────────────────┘
          │                 │                 │
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Express.js)                         │
│                   http://localhost:5000                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                  API ENDPOINTS                            │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │                                                           │ │
│  │  Authentication:                                          │ │
│  │  • POST /api/register  → Register new user               │ │
│  │  • POST /api/login     → Login and get JWT token         │ │
│  │                                                           │ │
│  │  Content Management:                                      │ │
│  │  • GET /api/content/home     → Home page content         │ │
│  │  • GET /api/content/team     → Team page content         │ │
│  │  • GET /api/content/about    → About page content        │ │
│  │  • GET /api/content/contact  → Contact page content      │ │
│  │  • GET /api/content/footer   → Footer content            │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                  MIDDLEWARE                               │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  • CORS                → Allow cross-origin requests      │ │
│  │  • express.json()      → Parse JSON bodies                │ │
│  │  • bcrypt              → Hash passwords                   │ │
│  │  • jsonwebtoken        → Generate/verify JWT tokens       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE (SQLite)                             │
│                   database.sqlite                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    USERS TABLE                            │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  id  │  username  │  email  │  password (hashed)         │ │
│  ├──────┼────────────┼─────────┼────────────────────────────┤ │
│  │  1   │  John Doe  │  john@  │  $2a$10$...               │ │
│  │  2   │  Jane S.   │  jane@  │  $2a$10$...               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                PAGE_CONTENT TABLE                         │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  id │ page_name │ section_name │ content_key │ value     │ │
│  ├─────┼───────────┼──────────────┼─────────────┼───────────┤ │
│  │  1  │  home     │  hero        │  fullText   │  WELCOME  │ │
│  │  2  │  home     │  hero        │  bgImage    │  https:// │ │
│  │  3  │  home     │  users       │  list       │  [...]    │ │
│  │  4  │  team     │  header      │  title      │  Our Team │ │
│  │  5  │  team     │  members     │  list       │  [...]    │ │
│  │  6  │  about    │  mission     │  text       │  To pro...│ │
│  │  7  │  contact  │  details     │  email      │  info@... │ │
│  │  8  │  footer   │  social      │  facebook   │  https:// │ │
│  │  9  │  footer   │  copyright   │  text       │  © 2026   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                          ▲
                          │
                          │
┌─────────────────────────┴───────────────────────────────────────┐
│              DATABASE MANAGEMENT TOOLS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  • manage-db.js        → CLI tool for database operations       │
│  • npm run db:users    → View all users                         │
│  • npm run db:content  → View all content                       │
│  • npm run db:stats    → Show statistics                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Authentication Flow

```
User Registration:
1. User fills form → Register.jsx
2. POST /api/register with {username, email, password}
3. Backend hashes password with bcrypt
4. Store in users table
5. Return success message

User Login:
1. User fills form → Login.jsx
2. POST /api/login with {email, password}
3. Backend validates credentials
4. Compare hashed passwords
5. Generate JWT token
6. Return {token, username}
7. Frontend stores token in localStorage
```

### Content Retrieval Flow

```
Page Load:
1. Component mounts (e.g., Home.jsx)
2. useEffect triggers fetch
3. GET /api/content/home
4. Backend queries page_content table
5. Filter by page_name = 'home'
6. Group by section_name
7. Parse JSON arrays/objects
8. Return structured data
9. Component updates state
10. Render with database content
```

---

## 📊 Database Schema Relationships

```
┌─────────────────────┐
│       USERS         │
│─────────────────────│
│ id (PK)             │
│ username            │
│ email (UNIQUE)      │
│ password (hashed)   │
└─────────────────────┘
         │
         │ (No direct relationship)
         │
┌─────────────────────┐
│   PAGE_CONTENT      │
│─────────────────────│
│ id (PK)             │
│ page_name           │
│ section_name        │
│ content_key         │
│ content_value       │
│                     │
│ UNIQUE(page_name,   │
│   section_name,     │
│   content_key)      │
└─────────────────────┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      SECURITY LAYERS                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: Password Security                                     │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • bcrypt hashing (10 salt rounds)                        │ │
│  │  • Passwords never stored in plain text                   │ │
│  │  • One-way encryption                                     │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Layer 2: Authentication                                        │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • JWT tokens (expires in 1 hour)                         │ │
│  │  • Token contains user ID and username                    │ │
│  │  • Signed with secret key                                 │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Layer 3: Database Security                                     │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • Prepared statements (SQL injection protection)         │ │
│  │  • Unique email constraint                                │ │
│  │  • Input validation                                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Layer 4: Network Security                                      │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • CORS enabled for frontend                              │ │
│  │  • JSON body parsing                                      │ │
│  │  • Error handling                                         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗂️ File Structure

```
Eric__/
│
├── server/                          # Backend directory
│   ├── index.js                     # Main Express server
│   ├── db.js                        # Database setup & seeding
│   ├── manage-db.js                 # Database management CLI
│   ├── database.sqlite              # SQLite database file
│   ├── package.json                 # Backend dependencies
│   ├── README.md                    # Server documentation
│   └── API_TESTING_GUIDE.md         # API testing guide
│
├── src/                             # Frontend directory
│   ├── component/
│   │   ├── Login.jsx                # Login component
│   │   ├── Register.jsx             # Registration component
│   │   ├── Home.jsx                 # Home page (DB-driven)
│   │   ├── Team.jsx                 # Team page (DB-driven)
│   │   ├── About.jsx                # About page (DB-driven)
│   │   ├── Contact.jsx              # Contact page (DB-driven)
│   │   └── Footer.jsx               # Footer (DB-driven)
│   │
│   ├── context/
│   │   └── AuthContext.jsx          # Authentication context
│   │
│   ├── App.jsx                      # Main app component
│   └── main.jsx                     # Entry point
│
├── BACKEND_DOCUMENTATION.md         # Complete backend docs
├── BACKEND_SUMMARY.md               # Backend summary
├── ARCHITECTURE.md                  # This file
├── QUICK_START.md                   # Quick start guide
├── AUTHENTICATION.md                # Auth documentation
└── package.json                     # Frontend dependencies
```

---

## 🔄 Component-to-API Mapping

```
Frontend Component          →  Backend Endpoint           →  Database Table
─────────────────────────────────────────────────────────────────────────────
Register.jsx                →  POST /api/register         →  users
Login.jsx                   →  POST /api/login            →  users
Home.jsx                    →  GET /api/content/home      →  page_content
Team.jsx                    →  GET /api/content/team      →  page_content
About.jsx                   →  GET /api/content/about     →  page_content
Contact.jsx                 →  GET /api/content/contact   →  page_content
Footer.jsx                  →  GET /api/content/footer    →  page_content
```

---

## 📈 Request/Response Flow

### Example: Loading Home Page

```
1. User navigates to home page
   ↓
2. Home.jsx component mounts
   ↓
3. useEffect hook triggers
   ↓
4. fetch('http://localhost:5000/api/content/home')
   ↓
5. Express server receives GET request
   ↓
6. Route handler: app.get('/api/content/:pageName')
   ↓
7. Query database: SELECT * FROM page_content WHERE page_name = 'home'
   ↓
8. Database returns rows:
   - hero.fullText
   - hero.bgImage
   - users.list
   - cards.image1
   - cards.image2
   ↓
9. Backend groups by section_name
   ↓
10. Backend parses JSON arrays
   ↓
11. Backend sends response:
    {
      "hero": { "fullText": "...", "bgImage": "..." },
      "users": { "list": [...] },
      "cards": { "image1": "...", "image2": "..." }
    }
   ↓
12. Home.jsx receives data
   ↓
13. setContent(data) updates state
   ↓
14. Component re-renders with database content
   ↓
15. User sees dynamic content
```

---

## 🎯 Key Features

### ✅ Implemented Features

1. **Authentication System**
   - User registration
   - User login
   - Password hashing
   - JWT token generation
   - Token-based authentication

2. **Content Management System**
   - Dynamic page content
   - Database-driven rendering
   - JSON array/object support
   - Grouped content by sections
   - Easy content updates

3. **Database Management**
   - CLI tool for operations
   - NPM scripts for convenience
   - View, add, update, delete
   - Statistics and reporting

4. **Security**
   - bcrypt password hashing
   - JWT authentication
   - SQL injection protection
   - CORS configuration
   - Error handling

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      PRODUCTION SETUP                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Frontend (React)                                               │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • Build: npm run build                                   │ │
│  │  • Deploy to: Vercel, Netlify, or static hosting         │ │
│  │  • Environment: VITE_API_URL=https://api.yourdomain.com  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Backend (Express)                                              │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • Deploy to: Heroku, Railway, or VPS                     │ │
│  │  • Environment variables:                                 │ │
│  │    - PORT=5000                                            │ │
│  │    - JWT_SECRET=strong-random-secret                      │ │
│  │    - NODE_ENV=production                                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Database (SQLite → PostgreSQL)                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • For production: Migrate to PostgreSQL or MySQL         │ │
│  │  • Use connection pooling                                 │ │
│  │  • Set up automated backups                               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Performance Considerations

```
┌─────────────────────────────────────────────────────────────────┐
│                    OPTIMIZATION STRATEGIES                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Frontend:                                                      │
│  • React.memo for components                                    │
│  • useMemo for expensive calculations                           │
│  • Lazy loading for routes                                      │
│  • Image optimization                                           │
│                                                                 │
│  Backend:                                                       │
│  • Database indexing                                            │
│  • Response caching                                             │
│  • Connection pooling                                           │
│  • Compression middleware                                       │
│                                                                 │
│  Database:                                                      │
│  • Indexed columns (email, page_name)                           │
│  • Prepared statements (already implemented)                    │
│  • Query optimization                                           │
│  • Regular VACUUM operations                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ System Status

🟢 **All Systems Operational**

- ✅ Frontend components connected
- ✅ Backend API endpoints working
- ✅ Database properly configured
- ✅ Authentication system functional
- ✅ Content management operational
- ✅ Security measures in place
- ✅ Documentation complete

**The system is production-ready!**
